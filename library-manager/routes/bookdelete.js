var express = require('express');
var router = express.Router();

/* GET home page. */
router.post('/books/:id/delete', function(req, res, next) {
 // delete book - make test book first
    //res.render(update-book', {id});
 
});

module.exports = router;
